version: 2

project_name: django-filerouter

before:
  hooks:
    - go mod tidy

builds:
  - id: django-filerouter
    main: ./cmd/django-filerouter
    binary: django-filerouter
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: darwin
        goarch: amd64

archives:
  - id: django-filerouter
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: AlmostWorkingSystem
    name: django-filerouter
